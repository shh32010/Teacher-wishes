-- ============================================================
-- 014 词库备注列 — 运营记录（如「甲方提供」「待替换」等）
-- ============================================================

ALTER TABLE blessing_templates ADD COLUMN IF NOT EXISTS remark TEXT;
